{
  "name": "uniswap",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ ianlapham/governance-tracking-bravo",
    "deploy-dev": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ arr00/uniswap-governance-v3",
    "create-local": "graph create --node http://localhost:8020/ arr00/uniswap-governance-v3",
    "remove-local": "graph remove --node http://localhost:8020/ arr00/uniswap-governance-v3",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 arr00/uniswap-governance-v3"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.20.0",
    "@graphprotocol/graph-ts": "^0.20.0"
  }
}
